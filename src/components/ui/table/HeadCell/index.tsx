import type { HeadCellProps } from '../../../../types';

import * as Styled from './styles';
export function HeadCell({ children }: HeadCellProps) {
  return <Styled.HeadCell>{children}</Styled.HeadCell>;
}
